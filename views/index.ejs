<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>

    <script>

        $(function () { // Dom Ready

            var myPath = [];

            $('button.choice').on('click', function (event) {

                $(this).parents('.question').addClass('question--hidden');

                var choiceId = $(this).data('step')

                $('#step' + choiceId).removeClass('question--hidden');

                myPath.push($(this).parents('.question').attr('id').replace('step', ''));

            });

            $('button.back').on('click', function (event) {

                $(this).parents('.question').addClass('question--hidden');

                var backId = myPath[myPath.length - 1];

                console.log(backId);

                $('#step' + backId).removeClass('question--hidden');

                myPath.length = myPath.length - 1;

            });

        });

    </script>

  </head>
  <body>

    <div id="step1" class="question">

        <p>Do you like interactive journeys?</p>

        <div class="related">some related info</div>

        <div class="choices">
            <button class="choice" data-step="2">yes</button>
            <button class="choice" data-step="3">no</button>
        </div>

    </div>

    <div id="step2" class="question question--hidden">

        <button class="back">< back</button>

        <p>Do you like second steps?</p>

        <div class="related">some related info</div>

        <div class="choices choices--hidden">
            <button class="choice">yes</button>
            <button class="choice">no</button>
        </div>

    </div>

    <div id="step3" class="question question--hidden">

        <button class="back">< back</button>

        <p>Do you like third steps?</p>

        <div class="related">some related info</div>

        <div class="choices choices--hidden">
            <button class="choice">yes</button>
            <button class="choice">no</button>
        </div>

    </div>

  </body>
</html>
